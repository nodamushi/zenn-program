
include $(APPDIR)/Make.defs
include $(SDKDIR)/Make.defs

PROGNAME = $(CONFIG_WORK_MY_HELLO_PROGNAME)
PRIORITY = $(CONFIG_WORK_MY_HELLO_PRIORITY)
STACKSIZE = $(CONFIG_WORK_MY_HELLO_STACKSIZE)
MODULE = $(CONFIG_WORK_MY_HELLO)

ASRCS =
CSRCS =
MAINSRC = my_hello_main.c

include $(APPDIR)/Application.mk
